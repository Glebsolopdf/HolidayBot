# Holiday Bot

Этот репозиторий содержит небольшого Telegram‑бота, который получает ежедневные праздники с calend.ru, предоставляет команду `/today`, поддерживает inline‑режим и выполняет ежедневный автопост с эмодзи и изменением названия чата.

Файлы документации

- Английская: `README.md`
- Русская: `README_ru.md` (этот файл)

Ключевые возможности

- `/today` — показывает праздники на сегодня; каждый праздник префиксируется эмодзи при совпадении фрагмента.
- Автопост — бот отправляет один выбранный праздник в формате `<emoji> Сегодня <праздник>!`, закрепляет его и открепляет предыдущий автопост.
- Эмодзи в названии чата — бот добавляет эмодзи в начало названия чата, сохраняя оригинальное название, если это возможно.

Словарь соответствия фрагментов и эмодзи теперь хранится в `holiday_emojis.json` рядом с `holiday_emojis.py`. Поведение:

- Если `holiday_emojis.json` отсутствует, модуль создаст файл с дефолтными парами фрагмент→эмодзи.
- Если файл существует и корректен, он будет использован. Если файл повреждён/некорректен, модуль залогирует ошибку и использует встроенные дефолты.
- Ожидаемый формат: JSON‑массив пар: `[ ["frag","emoji"], ... ]`. Фрагмент ищется как подстрока в названии (без учёта регистра).

Требования

- Python 3.13
- Зависимости: смотрите `requirements.txt` (`aiogram`, `aiohttp`, `python-dotenv`).

Примечания

- Для работы автопоста, закрепления сообщений и изменения названия чатов бот должен быть администратором с соответствующими правами.
- Сопоставление эмодзи выполняется по подстрокам; если нет подходящего эмодзи, используется 🎉 по умолчанию.
